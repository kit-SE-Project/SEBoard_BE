package com.seproject.recruit.domain.repository;

import com.seproject.recruit.domain.model.RecruitMainPageConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RecruitMainPageConfigRepository extends JpaRepository<RecruitMainPageConfig, Long> {
}
